# Mobile — App Oyente (Fase 3)

React Native + Expo con arquitectura **feature-first** (misma filosofía que web).

```
src/
  app/           # Navegación, providers
  features/      # player, search, library, auth
  entities/      # song, playlist
  shared/        # api (@streaming/api-client), ui
```

Pendiente — Fase 3.
